import { FC } from 'react';

interface NotFoundPageProps {
}

export const NotFoundPage: FC<NotFoundPageProps> = (props) => {
    return (
        <div>
            Some Text
        </div>
    );
};
