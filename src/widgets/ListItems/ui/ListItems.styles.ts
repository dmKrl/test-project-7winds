import { baseTheme } from 'shared/styles/theme';
import styled from 'styled-components';

export const ListItemsUI = styled.div`
    padding: 10px 0px;
    border-bottom: 1px solid ${baseTheme.colors.borderLine};
`;
