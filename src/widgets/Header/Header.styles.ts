import { baseTheme } from 'shared/styles/theme';
import styled from 'styled-components';

export const HeaderContainer = styled.header`
    height: ${baseTheme.sizes.header};
    background-color: ${baseTheme.colors.primary}
`;
